/**
 * A plugin for managing local declarations.
 * Keep in mind that this plugin is independent of Blockly's default variable,
 * and declared variables cannot be accessed by field_variable.
 * @module
 */

export * from './declaration_list';
export * from './builder';